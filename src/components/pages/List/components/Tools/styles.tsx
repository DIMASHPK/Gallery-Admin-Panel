import { css } from '@emotion/react';

export default () => ({
  toolsContainer: css({
    marginBottom: 30,
    width: '100%',
  }),
  toolsWrapper: css({
    display: 'flex',
    flexWrap: 'wrap',
    alignItems: 'center',
  }),
});
